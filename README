To run the application you have to set the working directory of the project
to the folder "project_base_code".
To do so, in QtCreator go under "Projects" (left menu), select the "run" tab,
and choose the correct working directory.
